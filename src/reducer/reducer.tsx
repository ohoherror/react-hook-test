const reducer = (state: any, action: any) => {
  return state;
  //   switch (action.type) {
  //     case "user/Update":
  //       return {
  //         ...state,
  //         user: action.user,
  //       };
  //   }
};

export default reducer;
